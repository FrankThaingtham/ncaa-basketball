unc_schedule_stats <- merge(unc_df, df_fixed_unc, by="game_id")
duke_schedule_stats <- merge(duke_df, df_fixed_duke, by="game_id")
ncstate_schedule_stats <- merge(ncstate_df, df_fixed_ncstate, by="game_id")

all_stats <- rbind(unc_schedule_stats, duke_schedule_stats, ncstate_schedule_stats )
